[phases.setup]
nixPkgs = ["python311", "nodejs_20", "gcc"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip", "cd backend && pip install -r requirements.txt", "cd frontend && npm ci"]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
