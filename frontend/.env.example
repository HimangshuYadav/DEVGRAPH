# DevGraph Frontend — Environment Variables

# Backend API URL (for client-side direct calls if any)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend URL for Next.js internal server-side rewrites / proxy
BACKEND_URL=http://localhost:8000
